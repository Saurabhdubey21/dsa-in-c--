#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<int>v(5,100);
    //it shows vector will store 100 ,5 times
    v.push_back(10);
    for(auto it :v){
        cout<<it<<" ";
// /we can add in pair as well
// vector<pair<int,int>v;
// v.push_back({1,2})
}
}